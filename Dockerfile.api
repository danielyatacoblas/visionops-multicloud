FROM python:3.12-slim
WORKDIR /app
COPY pyproject.toml README.md ./
COPY src ./src
RUN pip install --no-cache-dir .
ENV BULK_DATA_ROOT=/app/artifacts/api
EXPOSE 8000
CMD ["uvicorn", "visionops.api:app", "--host", "0.0.0.0", "--port", "8000"]
